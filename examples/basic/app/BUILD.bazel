load("@rules_uv_bare//uv:defs.bzl", "uv_py_package")

uv_py_package(
    name = "app",
    # Optionally, you can explicitly declare dependencies:
    # deps = ["//lib"],
    data = ["app/py.typed"] + glob(["app/data/**"]),
)
