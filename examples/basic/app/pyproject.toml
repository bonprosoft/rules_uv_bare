[project]
name = "app"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = ["lib", "click>=8.1"]

[project.scripts]
app = "app:run"

[build-system]
requires = ["uv_build>=0.7.19,<0.8.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
